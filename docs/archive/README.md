# Archived production artefacts

This directory holds the exact source of things that are, or were, running in
the production project but have no home in the live source tree. It exists for
one reason: this project's claim is that everything it runs is publicly
inspectable, and a deployed Edge Function whose source lives only inside the
hosting provider quietly breaks that claim.

Nothing in this directory is deployed by any workflow. It is a record.
